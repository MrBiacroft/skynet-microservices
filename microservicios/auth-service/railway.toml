[build]
builder = "NIXPACKS"
buildCommand = "mvn clean install -DskipTests && cd auth-service && mvn clean package -DskipTests"

[deploy]
startCommand = "java -Dserver.port=$PORT -jar auth-service/target/*.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
