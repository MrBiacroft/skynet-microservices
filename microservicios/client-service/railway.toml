[build]
builder = "NIXPACKS"
buildCommand = "mvn clean install -DskipTests && cd client-service && mvn clean package -DskipTests"

[deploy]
startCommand = "java -Dserver.port=$PORT -jar client-service/target/*.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
